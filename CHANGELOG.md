# Changelog

All notable changes to this project will be documented in this file.

## Unreleased

- Added repository maintenance files for linting and automated dependency checks.
- Updated project metadata and documentation to use current MagicMirror² naming and links.

## 2.2.0 - 2024-06-30

- Removed the deprecated `request` dependency in favor of built-in `fetch`.
- Limited sensor requests to only the configured `values`.
- Moved the control sensor into the `values` configuration block.
